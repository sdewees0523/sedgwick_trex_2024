---
title: "Untitled"
output: html_document
date: "2024-11-26"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(here)
library(tidyverse)
library(lubridate)
library(lme4)
library(lmerTest)
```

```{r}
temperatures <- read.csv(here("data", "clean_data", "temperature_clean.csv"))

max_temperature <- temperatures %>% 
  group_by(tree, branch) %>% 
  reframe(temperature = max(temperature_c)) %>% 
  mutate(branch = as.numeric(branch))

time_hot <- temperatures %>% 
  filter(temperature_c >= 30) %>% 
  group_by(tree, branch) %>% 
  reframe(minutes = n())%>% 
  mutate(branch = as.numeric(branch))

kleaf_efm <- read.csv(here("data", "clean_data", "kleaf_efm_clean.csv")) %>% 
  inner_join(max_temperature, by = c("tree", "branch")) %>% 
  left_join(time_hot, by = c("tree", "branch")) %>% 
  mutate(minutes = replace_na(minutes, 0))

kleaf_vpm <- read.csv(here("data", "clean_data", "kleaf_vpm_clean.csv")) %>% 
  inner_join(max_temperature, by = c("tree", "branch"))%>% 
  left_join(time_hot, by = c("tree", "branch")) %>% 
  mutate(minutes = replace_na(minutes, 0))

knative <- read.csv(here("data", "clean_data", "knative_clean.csv")) %>% 
  inner_join(max_temperature, by = c("tree", "branch"))%>% 
  left_join(time_hot, by = c("tree", "branch")) %>% 
  mutate(minutes = replace_na(minutes, 0))

kmax <- read.csv(here("data", "clean_data", "kmax_clean.csv")) %>% 
  inner_join(max_temperature, by = c("tree", "branch")) %>% 
  filter(kstem < 15)%>% 
  left_join(time_hot, by = c("tree", "branch")) %>% 
  mutate(minutes = replace_na(minutes, 0))

plc <- read.csv(here("data", "clean_data", "kstem_plc_clean.csv")) %>% 
  inner_join(max_temperature, by = c("tree", "branch"))%>% 
  left_join(time_hot, by = c("tree", "branch")) %>% 
  mutate(minutes = replace_na(minutes, 0))

gmin <- read.csv(here("data", "clean_data", "gmin_clean.csv")) %>% 
  inner_join(max_temperature, by = c("tree", "branch"))%>% 
  left_join(time_hot, by = c("tree", "branch")) %>% 
  mutate(minutes = replace_na(minutes, 0))
```

```{r}
ggplot(kleaf_efm, aes(x = temperature, y = kleaf))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()

ggplot(kleaf_vpm, aes(x = temperature, y = kleaf))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()

ggplot(knative, aes(x = temperature, y = kstem))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()

ggplot(kmax, aes(x = temperature, y = kstem))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()

ggplot(plc%>% filter(treatment == "burned"), aes(x = temperature, y = plc))+
  geom_point()+
  geom_smooth(method = "glm")+
  theme_classic()
```


```{r}
ggplot(kleaf_efm, aes(x = minutes, y = kleaf))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()+
  ylim(0,0.25)+
  xlim(0,25)

ggplot(kleaf_vpm, aes(x = minutes, y = kleaf))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()+
  ylim(0, 25)+
  xlim(0,25)

ggplot(knative %>% filter(treatment == "burned"), aes(x = minutes, y = kstem))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()+
  ylim(0,1.5)+
  xlim(0,25)

ggplot(kmax, aes(x = minutes, y = kstem))+
  geom_point()+
  geom_smooth(method = "glm",
              method.args = list(c("Gamma")))+
  theme_classic()+
  ylim(0,2.25)+
  xlim(0,25)

ggplot(plc%>% filter(treatment == "burned"), aes(x = minutes, y = plc))+
  geom_point()+
  geom_smooth(method = "glm")+
  theme_classic()
```


```{r}
ggplot(gmin, aes(x = temperature, y = gmin))+
  geom_point()+
  geom_smooth(method = "glm") +
  theme_classic()

test <- glm(gmin~temperature, family = "Gamma", data = gmin %>% filter(treatment == "burned"))

test_out <- tibble(temperature = seq(from = 16, to =85, by = 1)) %>% 
  mutate(tree = 66,
         gmin = predict(test, newdata = .)) 

ggplot(test_out, aes(x = temperature, y = gmin))+
  geom_line()+
  theme_classic()

summary(test)
```

